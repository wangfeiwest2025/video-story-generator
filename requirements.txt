# AI短视频生成器 - Python依赖

# Web界面
streamlit>=1.30.0

# TTS语音合成
edge-tts>=6.1.0

# 音频处理（MP3元数据读取）
mutagen>=1.47.0

# 网络请求（ComfyUI API调用）
requests>=2.31.0

# 图像处理
Pillow>=10.0.0

# ffmpeg 二进制备份方案（pip 自带静态 ffmpeg，云端容器无 apt 时可用）
imageio-ffmpeg>=0.5.1

# 注意：ffmpeg 优先通过系统包管理器安装
# Streamlit Cloud: 仓库根目录 packages.txt 写入 ffmpeg（自动 apt 安装）
# 本地 Windows: winget install --id Gyan.FFmpeg -e